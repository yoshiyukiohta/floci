# application.yml Reference

!!! note "Moved"
    This page has moved to [Advanced → application.yml Reference](./advanced/application-yml.md).

    If you run the published Docker image, you don't need an `application.yml` — all settings are configured through `FLOCI_*` environment variables. See the [Environment Variables Reference](./environment-variables.md).
